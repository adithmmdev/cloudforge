# Copy this to .env.local and set your Render backend URL to call Kimi K3.
# VITE_BACKEND_URL must NOT have a trailing slash.
# Leave empty to use the local NGINX proxy (default).
VITE_BACKEND_URL=https://cloudforge-backend.onrender.com
